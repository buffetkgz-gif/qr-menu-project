# 🔧 Исправления: Валюта и Баннеры

## ✅ Исправленные проблемы

### 1. Валюта не менялась везде

**Проблема:** При изменении валюты в настройках ресторана, она оставалась захардкоженной как "₸" (тенге) в следующих местах:

- Карточки блюд в управлении меню
- Форма добавления/редактирования блюда
- Метки полей "Стоимость доставки" и "Минимальная сумма заказа"

**Решение:**

- Добавлена динамическая загрузка валюты из данных ресторана
- Валюта теперь передается через props во все компоненты
- Все метки обновляются автоматически при изменении валюты

### 2. Баннер не отображался после загрузки

**Проблема:** После загрузки баннера он не отображался в настройках ресторана

**Решение:**

- Исправлено отображение баннеров (используется массив `banners` вместо `bannerUrl`)
- Добавлена сетка для отображения всех загруженных баннеров
- Добавлен индикатор выбранного файла
- Добавлена очистка поля после успешной загрузки

---

## 📝 Измененные файлы

### Frontend

#### 1. `frontend/src/pages/MenuManagementPage.jsx`

**Изменения:**

- ✅ Добавлено состояние `currency` для хранения валюты ресторана
- ✅ Валюта загружается из `data.restaurant.currency` при загрузке данных
- ✅ Валюта отображается в карточках блюд вместо захардкоженного "₸"
- ✅ Валюта передается в `DishModal` через props
- ✅ В `DishModal` метка поля "Цена" теперь динамическая: `Цена ({currency})`

**Код:**

```javascript
// Добавлено состояние
const [currency, setCurrency] = useState('₽');

// Загрузка валюты
const loadData = async () => {
  const data = await authService.getMe();
  if (data.restaurant) {
    setCurrency(data.restaurant.currency || '₽');
    // ...
  }
};

// Отображение в карточке блюда
<p className="text-primary-600 font-semibold">
  {dish.price} {currency}
</p>

// Передача в модальное окно
<DishModal
  dish={editingDish}
  categoryId={selectedCategoryId}
  currency={currency}
  // ...
/>

// В DishModal
const DishModal = ({ dish, categoryId, currency = '₽', onClose, onSave }) => {
  // ...
  <label>Цена ({currency})</label>
}
```

#### 2. `frontend/src/pages/RestaurantSettingsPage.jsx`

**Изменения:**

- ✅ Метки полей доставки теперь используют динамическую валюту
- ✅ Исправлено отображение баннеров (используется массив `banners`)
- ✅ Добавлена сетка для отображения всех баннеров
- ✅ Добавлен индикатор выбранного файла
- ✅ Файл очищается после успешной загрузки

**Код:**

```javascript
// Динамические метки
<label>Стоимость доставки ({currency})</label>
<label>Минимальная сумма заказа ({currency})</label>

// Отображение баннеров
{userData?.restaurant?.banners && userData.restaurant.banners.length > 0 && (
  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
    {userData.restaurant.banners.map((banner, index) => (
      <img src={banner} alt={`Banner ${index + 1}`} />
    ))}
  </div>
)}

// Индикатор выбранного файла
{bannerFile && (
  <p className="text-sm text-green-600 mt-1">
    ✓ Выбран файл: {bannerFile.name}
  </p>
)}

// Очистка после загрузки
if (bannerFile) {
  await restaurantService.uploadBanner(userData.restaurant.id, bannerFile);
  setBannerFile(null);
}
```

### Backend

#### 3. `backend/src/controllers/auth.controller.js`

**Изменения:**

- ✅ Добавлен парсинг баннеров в методе `getMe()`
- ✅ Баннеры конвертируются из JSON строки в массив (для совместимости с SQLite)

**Код:**

```javascript
export const getMe = async (req, res, next) => {
  // ...

  // Parse banners if it's a JSON string (SQLite compatibility)
  if (
    user?.restaurant?.banners &&
    typeof user.restaurant.banners === "string"
  ) {
    try {
      user.restaurant.banners = JSON.parse(user.restaurant.banners);
    } catch (e) {
      user.restaurant.banners = [];
    }
  }

  res.json(user);
};
```

---

## 🧪 Как протестировать

### Тест 1: Изменение валюты

1. Запустите проект:

   ```powershell
   npm run dev
   ```

2. Войдите в систему как владелец ресторана

3. Перейдите в **Настройки ресторана**

4. Измените валюту (например, с ₽ на $)

5. Нажмите **Сохранить изменения**

6. Перейдите в **Управление меню**

7. **Проверьте:**
   - ✅ Цены блюд отображаются с новой валютой
   - ✅ При добавлении/редактировании блюда метка "Цена" показывает новую валюту
8. Вернитесь в **Настройки ресторана**

9. **Проверьте:**

   - ✅ Метки "Стоимость доставки" и "Минимальная сумма заказа" показывают новую валюту

10. Откройте публичное меню: `http://localhost:5173/menu/[ваш-subdomain]`

11. **Проверьте:**
    - ✅ Все цены отображаются с новой валютой
    - ✅ В корзине цены с новой валютой
    - ✅ В модальном окне блюда цены с новой валютой

### Тест 2: Загрузка баннера

1. Перейдите в **Настройки ресторана**

2. В секции "Баннеры" нажмите **Выбрать файл**

3. Выберите изображение (JPG, PNG, WEBP)

4. **Проверьте:**

   - ✅ Появилось сообщение "✓ Выбран файл: [имя файла]"

5. Нажмите **Сохранить изменения**

6. Дождитесь сообщения "Настройки сохранены!"

7. **Проверьте:**

   - ✅ Загруженный баннер отображается в секции "Текущие баннеры"
   - ✅ Если было несколько баннеров, все отображаются в сетке

8. Откройте публичное меню: `http://localhost:5173/menu/[ваш-subdomain]`

9. **Проверьте:**
   - ✅ Баннер отображается в слайдере вверху страницы
   - ✅ Если несколько баннеров, слайдер автоматически переключается между ними

---

## 🎯 Результат

### До исправления:

- ❌ Валюта "₸" была захардкожена в нескольких местах
- ❌ При изменении валюты в настройках, она не менялась в меню
- ❌ Баннеры не отображались после загрузки

### После исправления:

- ✅ Валюта загружается динамически из базы данных
- ✅ При изменении валюты она обновляется везде автоматически
- ✅ Баннеры корректно отображаются в настройках и на публичной странице
- ✅ Можно загружать несколько баннеров
- ✅ Все баннеры отображаются в сетке в настройках

---

## 📊 Технические детали

### Поток данных валюты:

1. **База данных** → Поле `currency` в таблице `restaurants` (по умолчанию "₽")
2. **Backend API** → Возвращает валюту в объекте ресторана
3. **Frontend State** → Сохраняется в состоянии компонента
4. **UI Components** → Отображают валюту динамически

### Поток данных баннеров:

1. **База данных** → Поле `banners` в таблице `restaurants` (JSON строка в SQLite)
2. **Backend API** → Парсит JSON строку в массив перед отправкой
3. **Frontend** → Получает массив URL баннеров
4. **UI** → Отображает все баннеры в сетке или слайдере

---

## 🔍 Дополнительные улучшения

### Возможные будущие улучшения:

1. **Удаление баннеров**

   - Добавить кнопку удаления для каждого баннера
   - Использовать существующий endpoint `/delete-banner`

2. **Предпросмотр баннера**

   - Показывать предпросмотр выбранного файла перед загрузкой

3. **Порядок баннеров**

   - Добавить возможность изменять порядок баннеров (drag & drop)

4. **Форматирование цен**
   - Добавить форматирование чисел в зависимости от валюты
   - Например: 1,000.00 для $ и 1 000,00 для ₽

---

## ✨ Заключение

Все проблемы с валютой и баннерами успешно исправлены! Теперь:

- Валюта работает динамически во всех частях приложения
- Баннеры корректно загружаются и отображаются
- Код стал более гибким и поддерживаемым

Приятного использования! 🎉
