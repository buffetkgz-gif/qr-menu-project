# ✨ Добавлены индикаторы загрузки фотографий

## 🎯 Что было добавлено

Теперь при загрузке фотографий вы будете видеть:

### 1. **Индикатор выбранного файла** ✅

После выбора файла появляется зелёное сообщение:

```
✓ Выбран файл: photo.jpg
```

### 2. **Анимированный индикатор загрузки** 🔄

Во время загрузки появляется синяя панель с анимацией:

```
🔄 Загрузка баннера...
🔄 Загрузка изображения...
```

### 3. **Обновление текста кнопки** 💾

Кнопка "Сохранить" меняет текст в зависимости от процесса:

- **"Сохранить изменения"** - обычное состояние
- **"Сохранение..."** - сохранение данных
- **"Загрузка баннера..."** - загрузка баннера
- **"Загрузка фото..."** - загрузка фото блюда

---

## 📍 Где добавлены индикаторы

### 1. Настройки ресторана (RestaurantSettingsPage)

**Секция "Баннеры":**

✅ **Выбор файла:**

```
Загрузить новый баннер
[Выбрать файл]
✓ Выбран файл: banner.jpg  ← Зелёное сообщение
```

✅ **Во время загрузки:**

```
┌─────────────────────────────────┐
│ 🔄 Загрузка баннера...          │  ← Синяя панель с анимацией
└─────────────────────────────────┘
```

✅ **Кнопка:**

```
[Отмена]  [Загрузка баннера...]  ← Кнопка заблокирована
```

---

### 2. Управление меню (MenuManagementPage - DishModal)

**Форма добавления/редактирования блюда:**

✅ **Выбор файла:**

```
Фото
[Выбрать файл]
✓ Выбран файл: pizza.jpg  ← Зелёное сообщение
```

✅ **Во время загрузки:**

```
┌─────────────────────────────────┐
│ 🔄 Загрузка изображения...      │  ← Синяя панель с анимацией
└─────────────────────────────────┘
```

✅ **Кнопка:**

```
[Отмена]  [Загрузка фото...]  ← Кнопка заблокирована
```

---

## 🎨 Визуальный дизайн

### Индикатор выбранного файла

```css
Цвет: Зелёный (#10B981)
Иконка: ✓
Размер текста: Маленький (text-sm)
```

### Индикатор загрузки

```css
Фон: Светло-синий (#EFF6FF)
Граница: Синяя (#BFDBFE)
Анимация: Вращающийся спиннер
Цвет текста: Синий (#2563EB)
```

---

## 🧪 Как протестировать

### Тест 1: Загрузка баннера

1. Откройте: http://localhost:5173/settings
2. Прокрутите до секции "Баннеры"
3. Нажмите **"Выбрать файл"**
4. Выберите изображение

**Ожидаемый результат:**

```
✅ Появилось: "✓ Выбран файл: [имя_файла]"
```

5. Нажмите **"Сохранить изменения"**

**Ожидаемый результат:**

```
✅ Кнопка изменилась на: "Сохранение..."
✅ Появилась синяя панель: "🔄 Загрузка баннера..."
✅ Кнопка изменилась на: "Загрузка баннера..."
✅ После загрузки: баннер появился в секции "Текущие баннеры"
✅ Индикатор выбранного файла исчез
```

---

### Тест 2: Загрузка фото блюда

1. Откройте: http://localhost:5173/menu-management
2. Нажмите **"+ Добавить блюдо"**
3. Заполните название и цену
4. Нажмите **"Выбрать файл"** в поле "Фото"
5. Выберите изображение

**Ожидаемый результат:**

```
✅ Появилось: "✓ Выбран файл: [имя_файла]"
```

6. Нажмите **"Сохранить"**

**Ожидаемый результат:**

```
✅ Кнопка изменилась на: "Сохранение..."
✅ Появилась синяя панель: "🔄 Загрузка изображения..."
✅ Кнопка изменилась на: "Загрузка фото..."
✅ После загрузки: модальное окно закрылось
✅ Блюдо появилось с загруженным изображением
```

---

## 📝 Технические детали

### Изменённые файлы:

#### 1. `frontend/src/pages/RestaurantSettingsPage.jsx`

**Добавлено:**

```javascript
// Новое состояние
const [uploadingBanner, setUploadingBanner] = useState(false);

// В handleSubmit
if (bannerFile) {
  setUploadingBanner(true);
  try {
    await restaurantService.uploadBanner(userData.restaurant.id, bannerFile);
    setBannerFile(null);
  } finally {
    setUploadingBanner(false);
  }
}

// В JSX
{bannerFile && !uploadingBanner && (
  <p className="text-sm text-green-600 mt-1">
    ✓ Выбран файл: {bannerFile.name}
  </p>
)}

{uploadingBanner && (
  <div className="mt-2 p-3 bg-blue-50 border border-blue-200 rounded-lg">
    <div className="flex items-center gap-2">
      <div className="animate-spin rounded-full h-5 w-5 border-b-2 border-blue-600"></div>
      <span className="text-sm text-blue-600 font-medium">
        Загрузка баннера...
      </span>
    </div>
  </div>
)}

// Кнопка
disabled={saving || uploadingBanner}
{uploadingBanner ? 'Загрузка баннера...' : saving ? 'Сохранение...' : 'Сохранить изменения'}
```

---

#### 2. `frontend/src/pages/MenuManagementPage.jsx`

**Добавлено в DishModal:**

```javascript
// Новое состояние
const [uploadingImage, setUploadingImage] = useState(false);

// В handleSubmit
if (imageFile) {
  setUploadingImage(true);
  try {
    await menuService.uploadDishImage(savedDish.id, imageFile);
  } finally {
    setUploadingImage(false);
  }
}

// В JSX
{imageFile && !uploadingImage && (
  <p className="text-sm text-green-600 mt-1">
    ✓ Выбран файл: {imageFile.name}
  </p>
)}

{uploadingImage && (
  <div className="mt-2 p-3 bg-blue-50 border border-blue-200 rounded-lg">
    <div className="flex items-center gap-2">
      <div className="animate-spin rounded-full h-5 w-5 border-b-2 border-blue-600"></div>
      <span className="text-sm text-blue-600 font-medium">
        Загрузка изображения...
      </span>
    </div>
  </div>
)}

// Кнопки
disabled={saving || uploadingImage}
{uploadingImage ? 'Загрузка фото...' : saving ? 'Сохранение...' : 'Сохранить'}
```

---

## 🎯 Преимущества

### До изменений:

❌ Не было понятно, выбран ли файл
❌ Не было видно, что идёт загрузка
❌ Пользователь мог случайно закрыть страницу во время загрузки
❌ Не было обратной связи о процессе

### После изменений:

✅ Сразу видно, какой файл выбран
✅ Чёткий индикатор процесса загрузки
✅ Кнопки заблокированы во время загрузки
✅ Понятный текст на кнопках
✅ Профессиональный UX

---

## 🔄 Последовательность состояний

### Загрузка баннера:

```
1. Начальное состояние
   [Выбрать файл]
   [Сохранить изменения]

2. Файл выбран
   ✓ Выбран файл: banner.jpg
   [Сохранить изменения]

3. Сохранение данных
   ✓ Выбран файл: banner.jpg
   [Сохранение...] ← заблокирована

4. Загрузка баннера
   🔄 Загрузка баннера...
   [Загрузка баннера...] ← заблокирована

5. Завершено
   [Выбрать файл]
   [Сохранить изменения]
   + Баннер отображается в списке
```

---

## 💡 Дополнительные улучшения (будущее)

### Возможные улучшения:

1. **Прогресс-бар загрузки**

   ```
   Загрузка баннера... [████████░░] 80%
   ```

2. **Предпросмотр выбранного изображения**

   ```
   [Миниатюра выбранного файла]
   ✓ Выбран файл: banner.jpg
   ```

3. **Размер файла**

   ```
   ✓ Выбран файл: banner.jpg (2.5 MB)
   ```

4. **Успешная загрузка**

   ```
   ✅ Баннер успешно загружен!
   ```

5. **Ошибка загрузки**
   ```
   ❌ Ошибка загрузки. Попробуйте снова.
   ```

---

## ✨ Заключение

Теперь загрузка фотографий стала намного понятнее и удобнее! Пользователь всегда знает:

- Какой файл выбран
- Идёт ли загрузка
- На каком этапе процесс
- Когда загрузка завершена

Приятного использования! 🎉
