# 📍 Статус геолокационной системы

## ✅ ТЕКУЩЕЕ СОСТОЯНИЕ: АКТИВНА (для проверки зоны доставки)

Геолокация используется для **проверки зоны доставки** при оформлении заказа.

---

## ✅ Что работает сейчас

### Backend (готов полностью):

- ✅ База данных расширена полями: `latitude`, `longitude`, `deliveryRadius`
- ✅ API `/api/geolocation/nearby` - поиск ближайших ресторанов
- ✅ API `/api/geolocation/nearest` - найти самый близкий ресторан
- ✅ API `/api/geolocation/check-delivery` - проверка зоны доставки ⭐ **АКТИВНО ИСПОЛЬЗУЕТСЯ**
- ✅ Формула Haversine для расчёта расстояний
- ✅ Сохранение координат клиента в заказе (deliveryLatitude, deliveryLongitude)

### Frontend (админ-панель):

- ✅ Поля для ввода координат в настройках ресторана
- ✅ Поле для радиуса доставки (км)
- ✅ Переключатель "Включить доставку" (`deliveryEnabled`)
- ✅ Поле стоимости доставки (`deliveryFee`)
- ✅ Селектор ресторанов для владельца (без геолокации)

### Frontend (клиенты):

- ✅ Прямой переход по ссылкам: `/menu/subdomain` или `/subdomain`
- ✅ **Форма доставки в корзине** (если `deliveryEnabled = true`) ⭐ **НОВОЕ**
- ✅ **Запрос GPS координат при оформлении заказа** ⭐ **НОВОЕ**
- ✅ **Проверка попадания в радиус доставки** ⭐ **НОВОЕ**
- ✅ **Блокировка заказа если вне зоны доставки** ⭐ **НОВОЕ**
- ❌ Страница `/select-restaurant` удалена (не нужна)

---

## 🎯 Текущая логика работы

### Владелец ресторана:

1. Заходит в админ-панель
2. В настройках ресторана (`/admin/settings`):
   - ✅ Включает доставку (`deliveryEnabled = true`)
   - ✅ Указывает координаты ресторана (широта, долгота)
   - ✅ Устанавливает радиус доставки (например, 5 км)
   - ✅ Опционально: стоимость доставки (например, 200 ₽)
   - ✅ Указывает WhatsApp для приёма заказов
3. Распространяет прямую ссылку на своё меню

### Клиент (если доставка ВКЛЮЧЕНА):

1. Получает ссылку от владельца (WhatsApp, Instagram, QR-код)
2. Переходит по ссылке: `https://ваш-сайт/menu/название-ресторана`
3. Выбирает блюда и добавляет в корзину
4. Открывает корзину - видит **форму доставки**:
   - 👤 Поле "Ваше имя"
   - 📱 Поле "Ваш телефон"
   - 📍 Поле "Адрес доставки"
   - 📍 Кнопка "Определить моё местоположение"
5. Заполняет форму и нажимает **"Определить моё местоположение"**
6. Браузер запрашивает разрешение на доступ к геолокации
7. Система получает GPS координаты и отправляет на `/api/geolocation/check-delivery`
8. Backend проверяет расстояние:
   - ✅ **В зоне доставки** (distance ≤ deliveryRadius):
     - Показывает зелёное сообщение: "Доставка доступна"
     - Показывает расстояние: "2.5 км (макс. 5 км)"
     - Разрешает оформление заказа
   - ❌ **Вне зоны доставки** (distance > deliveryRadius):
     - Показывает красное сообщение: "Вы находитесь за пределами зоны доставки"
     - Показывает расстояние: "7.2 км (макс. 5 км)"
     - **Блокирует оформление заказа**
9. Если в зоне - нажимает "Оформить заказ в WhatsApp"
10. Заказ создаётся с данными доставки:
    - Имя, телефон, адрес
    - Координаты клиента
    - Расстояние до ресторана
11. WhatsApp открывается с полным текстом заказа

### Клиент (если доставка ОТКЛЮЧЕНА):

1. Получает ссылку
2. Выбирает блюда
3. Оформляет заказ **без формы доставки** (как раньше)

---

## 📱 Пример сообщения в WhatsApp (с доставкой)

```
Здравствуйте! Мой номер заказа: #1234567890

Ресторан: Суши Бар (ул. Ленина, 10)

👤 Имя: Иван Петров
📱 Телефон: +7 999 123-45-67
📍 Адрес доставки: ул. Пушкина, д. 5, кв. 10
🚗 Расстояние: 2.5 км

Хочу сделать заказ:

2x Филадельфия - 800.00 ₽
1x Калифорния - 450.00 ₽
1x Том Ям - 350.00 ₽

Итого: 1600.00 ₽
Доставка: 200.00 ₽
```

---

## 🛠️ Техническая реализация

### Frontend - Cart.jsx

**Состояния:**

```javascript
const [customerName, setCustomerName] = useState("");
const [customerPhone, setCustomerPhone] = useState("");
const [deliveryAddress, setDeliveryAddress] = useState("");
const [userLocation, setUserLocation] = useState(null);
const [deliveryCheck, setDeliveryCheck] = useState(null);
```

**Проверка геолокации:**

```javascript
const handleGetLocation = () => {
  navigator.geolocation.getCurrentPosition(async (position) => {
    const { latitude, longitude } = position.coords;
    const response = await api.get("/geolocation/check-delivery", {
      params: { restaurantId, latitude, longitude },
    });
    setDeliveryCheck(response.data);
  });
};
```

**Валидация при оформлении:**

```javascript
if (restaurant.deliveryEnabled) {
  if (!customerName || !customerPhone || !deliveryAddress) {
    alert("Заполните все поля");
    return;
  }
  if (!deliveryCheck?.deliveryAvailable) {
    alert("Доставка недоступна в вашей зоне");
    return;
  }
}
```

### Backend - orders.controller.js

**Сохранение данных доставки:**

```javascript
const orderData = {
  orderNumber,
  restaurantId,
  items: JSON.stringify(items),
  total,
  customerName: customerName || "Клиент",
  customerPhone: customerPhone || "Не указан",
  deliveryAddress,
  deliveryLatitude: parseFloat(latitude),
  deliveryLongitude: parseFloat(longitude),
};
```

### Backend - geolocation.controller.js

**Проверка зоны доставки:**

```javascript
const distance = calculateDistance(
  userLat,
  userLon,
  restaurant.latitude,
  restaurant.longitude
);

const isInDeliveryZone = restaurant.deliveryRadius
  ? distance <= restaurant.deliveryRadius
  : true;

const deliveryAvailable = restaurant.deliveryEnabled && isInDeliveryZone;
```

---

## 📊 Поля в базе данных

### Restaurant:

```prisma
latitude          Float?    // Координаты ресторана
longitude         Float?
deliveryRadius    Float?    // Радиус доставки в км
deliveryEnabled   Boolean   // Включена ли доставка
deliveryFee       Float?    // Стоимость доставки
```

### Order:

```prisma
customerName      String    // Имя клиента
customerPhone     String    // Телефон клиента
customerEmail     String?   // Email (опционально)
deliveryAddress   String?   // Адрес доставки
deliveryLatitude  Float?    // GPS координаты клиента
deliveryLongitude Float?
```

---

## 🔮 Возможности для будущего

Backend готов для:

1. ✅ Показ всех заказов с координатами на карте (админка)
2. ✅ Визуализация зон доставки (Google Maps / Yandex Maps)
3. ✅ Автоматический расчёт стоимости доставки по расстоянию
4. ✅ Страница выбора ближайшего ресторана (если рестораны сеть)
5. ✅ Мобильное приложение с геолокацией
6. ✅ Push-уведомления курьерам с координатами клиента

---

## 📁 Изменённые файлы

### Frontend:

- ✅ `frontend/src/components/Cart.jsx` - добавлена форма доставки и геолокация

### Backend:

- ✅ `backend/src/controllers/orders.controller.js` - сохранение данных доставки
- ✅ `backend/src/controllers/geolocation.controller.js` - уже готов
- ✅ `backend/prisma/schema.prisma` - поля уже добавлены

### Документация:

- ✅ `GEOLOCATION_STATUS.md` - обновлён
- 🔄 `GEOLOCATION_README.md` - требует обновления
- 🔄 `docs/guides/GEOLOCATION_SETUP.md` - требует обновления

---

## ✅ Преимущества текущей реализации

1. ✅ **Не запрашивает GPS у всех клиентов** - только у тех, кто заказывает доставку
2. ✅ **Не мешает обычным заказам** - если доставка отключена, форма не показывается
3. ✅ **Защищает владельца** - клиенты вне зоны доставки не могут оформить заказ
4. ✅ **Информативно** - показывает расстояние и радиус доставки
5. ✅ **Сохраняет данные** - координаты сохраняются для аналитики и логистики
6. ✅ **Гибко** - владелец может включить/отключить доставку в настройках

---

**Дата последнего обновления:** 2025-01-XX  
**Статус:** ✅ Геолокация АКТИВНА для проверки зоны доставки
