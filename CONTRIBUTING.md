# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту QR Menu SaaS! Мы рады любому вкладу.

---

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как я могу помочь?](#как-я-могу-помочь)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)
- [Отчеты об ошибках](#отчеты-об-ошибках)
- [Предложения функций](#предложения-функций)

---

## 📜 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать наш кодекс поведения:

- Будьте уважительны к другим участникам
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам

---

## 🎯 Как я могу помочь?

### Для начинающих

Ищите issues с метками:

- `good first issue` - хорошо для первого вклада
- `help wanted` - нужна помощь
- `documentation` - улучшение документации

### Для опытных разработчиков

- Реализация новых функций из [TODO.md](./TODO.md)
- Оптимизация производительности
- Рефакторинг кода
- Написание тестов
- Code review

### Для дизайнеров

- Улучшение UI/UX
- Создание иконок и графики
- Дизайн новых компонентов
- Адаптивный дизайн

### Для технических писателей

- Улучшение документации
- Написание туториалов
- Перевод на другие языки
- Создание видео-гайдов

---

## 🔄 Процесс разработки

### 1. Fork репозитория

Нажмите кнопку "Fork" в правом верхнем углу GitHub.

### 2. Клонируйте свой fork

```powershell
git clone https://github.com/YOUR_USERNAME/qr-menu-saas.git
Set-Location qr-menu-saas
```

### 3. Добавьте upstream remote

```powershell
git remote add upstream https://github.com/ORIGINAL_OWNER/qr-menu-saas.git
```

### 4. Создайте ветку для вашей функции

```powershell
git checkout -b feature/your-feature-name
```

Именование веток:

- `feature/` - новая функция
- `fix/` - исправление бага
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

### 5. Внесите изменения

Следуйте [стандартам кода](#стандарты-кода).

### 6. Коммитьте изменения

```powershell
git add .
git commit -m "feat: add new feature"
```

Следуйте [правилам коммитов](#коммиты).

### 7. Синхронизируйте с upstream

```powershell
git fetch upstream
git rebase upstream/main
```

### 8. Отправьте изменения

```powershell
git push origin feature/your-feature-name
```

### 9. Создайте Pull Request

Перейдите на GitHub и создайте Pull Request из вашей ветки в main.

---

## 💻 Стандарты кода

### JavaScript/React

#### Общие правила

- Используйте ES6+ синтаксис
- Предпочитайте `const` и `let` вместо `var`
- Используйте стрелочные функции где возможно
- Деструктуризация объектов и массивов
- Template literals для строк

#### Именование

```javascript
// Переменные и функции: camelCase
const userName = "John";
function getUserData() {}

// Компоненты React: PascalCase
function UserProfile() {}

// Константы: UPPER_SNAKE_CASE
const API_URL = "http://localhost:5000";

// Приватные методы: _camelCase
function _privateMethod() {}
```

#### React компоненты

```javascript
// Функциональные компоненты
import React from "react";

function MyComponent({ prop1, prop2 }) {
  // Hooks в начале
  const [state, setState] = useState(null);
  const navigate = useNavigate();

  // Обработчики событий
  const handleClick = () => {
    // ...
  };

  // Эффекты
  useEffect(() => {
    // ...
  }, []);

  // Ранний return для условий
  if (!data) return <div>Loading...</div>;

  // JSX
  return <div className="container">{/* ... */}</div>;
}

export default MyComponent;
```

#### Стили

```javascript
// Используйте TailwindCSS классы
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow">
  {/* ... */}
</div>;

// Для динамических классов
const buttonClass = `
  px-4 py-2 rounded
  ${isActive ? "bg-blue-500" : "bg-gray-500"}
  ${isDisabled && "opacity-50 cursor-not-allowed"}
`;
```

### Backend (Node.js/Express)

#### Структура контроллера

```javascript
// controllers/example.controller.js
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export const getItems = async (req, res) => {
  try {
    const items = await prisma.item.findMany();
    res.json(items);
  } catch (error) {
    console.error("Error fetching items:", error);
    res.status(500).json({ message: "Server error" });
  }
};

export const createItem = async (req, res) => {
  try {
    const { name, description } = req.body;

    // Валидация
    if (!name) {
      return res.status(400).json({ message: "Name is required" });
    }

    const item = await prisma.item.create({
      data: { name, description },
    });

    res.status(201).json(item);
  } catch (error) {
    console.error("Error creating item:", error);
    res.status(500).json({ message: "Server error" });
  }
};
```

#### Структура маршрута

```javascript
// routes/example.routes.js
import express from "express";
import { getItems, createItem } from "../controllers/example.controller.js";
import { protect } from "../middleware/auth.js";

const router = express.Router();

router.get("/", getItems);
router.post("/", protect, createItem);

export default router;
```

### Prisma схема

```prisma
model Example {
  id          Int      @id @default(autoincrement())
  name        String   @db.VarChar(100)
  description String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Отношения
  userId      Int
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  // Индексы
  @@index([userId])
}
```

---

## 📝 Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы

- `feat` - новая функция
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor` - рефакторинг кода
- `test` - добавление тестов
- `chore` - обновление зависимостей, конфигурации и т.д.
- `perf` - улучшение производительности

### Примеры

```bash
# Новая функция
git commit -m "feat(menu): add dish search functionality"

# Исправление бага
git commit -m "fix(auth): resolve token expiration issue"

# Документация
git commit -m "docs(api): update authentication endpoints"

# Рефакторинг
git commit -m "refactor(components): extract DishCard logic"

# С телом коммита
git commit -m "feat(cart): add quantity selector

- Add increment/decrement buttons
- Update total price calculation
- Add validation for max quantity"

# Breaking change
git commit -m "feat(api): change authentication flow

BREAKING CHANGE: JWT tokens now expire after 7 days instead of 30"
```

---

## 🔀 Pull Requests

### Чеклист перед созданием PR

- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлены новые тесты (если применимо)
- [ ] Документация обновлена
- [ ] Нет console.log и debug кода
- [ ] Коммиты следуют Conventional Commits
- [ ] Ветка синхронизирована с main

### Шаблон PR

```markdown
## Описание

Краткое описание изменений.

## Тип изменений

- [ ] Bug fix (исправление бага)
- [ ] New feature (новая функция)
- [ ] Breaking change (изменение, ломающее обратную совместимость)
- [ ] Documentation update (обновление документации)

## Как протестировать?

1. Шаг 1
2. Шаг 2
3. Ожидаемый результат

## Скриншоты (если применимо)

Добавьте скриншоты для UI изменений.

## Чеклист

- [ ] Код следует стандартам проекта
- [ ] Самостоятельно протестировал изменения
- [ ] Обновил документацию
- [ ] Изменения не генерируют новых предупреждений
- [ ] Добавил тесты (если применимо)

## Связанные issues

Closes #123
```

### Процесс review

1. Создайте PR с подробным описанием
2. Дождитесь code review от мейнтейнеров
3. Внесите запрошенные изменения
4. После одобрения PR будет смержен

---

## 🐛 Отчеты об ошибках

### Перед созданием issue

1. Проверьте, что issue еще не существует
2. Убедитесь, что используете последнюю версию
3. Попробуйте воспроизвести на чистой установке

### Шаблон bug report

```markdown
## Описание бага

Четкое и краткое описание проблемы.

## Шаги для воспроизведения

1. Перейти на '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

## Ожидаемое поведение

Что должно было произойти.

## Фактическое поведение

Что произошло на самом деле.

## Скриншоты

Если применимо, добавьте скриншоты.

## Окружение

- OS: [например, Windows 11]
- Browser: [например, Chrome 120]
- Node.js: [например, v18.17.0]
- Version: [например, 1.0.0]

## Дополнительный контекст

Любая другая информация о проблеме.

## Логи
```

Вставьте логи ошибок здесь

```

```

---

## 💡 Предложения функций

### Шаблон feature request

```markdown
## Описание функции

Четкое и краткое описание желаемой функции.

## Проблема, которую решает

Какую проблему решает эта функция?

## Предлагаемое решение

Как вы видите реализацию этой функции?

## Альтернативы

Какие альтернативные решения вы рассматривали?

## Дополнительный контекст

Скриншоты, примеры из других приложений и т.д.

## Приоритет

- [ ] Критично
- [ ] Высокий
- [ ] Средний
- [ ] Низкий
- [ ] Nice to have
```

---

## 🧪 Тестирование

### Запуск тестов

```powershell
# Backend тесты
Set-Location backend
npm test

# Frontend тесты
Set-Location frontend
npm test

# E2E тесты
npm run test:e2e
```

### Написание тестов

```javascript
// Backend (Jest)
describe("Auth Controller", () => {
  test("should register new user", async () => {
    const userData = {
      email: "test@example.com",
      password: "test123",
      restaurantName: "Test Restaurant",
    };

    const response = await request(app)
      .post("/api/auth/register")
      .send(userData);

    expect(response.status).toBe(201);
    expect(response.body).toHaveProperty("token");
  });
});

// Frontend (React Testing Library)
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";

test("renders login form", () => {
  render(<LoginPage />);

  expect(screen.getByLabelText(/email/i)).toBeInTheDocument();
  expect(screen.getByLabelText(/password/i)).toBeInTheDocument();
  expect(screen.getByRole("button", { name: /login/i })).toBeInTheDocument();
});
```

---

## 📚 Документация

### Комментарии в коде

```javascript
/**
 * Создает новое блюдо в меню
 * @param {Object} req - Express request object
 * @param {Object} req.body - Данные блюда
 * @param {string} req.body.name - Название блюда
 * @param {number} req.body.price - Цена блюда
 * @param {Object} res - Express response object
 * @returns {Promise<void>}
 */
export const createDish = async (req, res) => {
  // ...
};
```

### README для новых модулей

Если добавляете новый модуль, создайте README.md с:

- Описанием модуля
- Примерами использования
- API документацией
- Зависимостями

---

## 🎨 UI/UX Guidelines

### Дизайн принципы

1. **Простота** - интерфейс должен быть интуитивным
2. **Консистентность** - используйте существующие компоненты
3. **Отзывчивость** - все должно работать на мобильных
4. **Доступность** - следуйте WCAG 2.1 AA
5. **Производительность** - оптимизируйте изображения и код

### Цветовая схема

Используйте цвета из `tailwind.config.js`:

- Primary: оранжевые оттенки
- Success: зеленый
- Error: красный
- Warning: желтый
- Info: синий

### Типографика

- Заголовки: `text-2xl`, `text-3xl`, `text-4xl`
- Основной текст: `text-base`
- Мелкий текст: `text-sm`
- Шрифт: системный (sans-serif)

---

## 🔒 Безопасность

### Отчеты о уязвимостях

**НЕ создавайте публичные issues для уязвимостей!**

Отправьте email на: security@qrmenu.com (замените на реальный)

Включите:

- Описание уязвимости
- Шаги для воспроизведения
- Потенциальное влияние
- Предлагаемое исправление (если есть)

---

## 📞 Контакты

- GitHub Issues: для багов и feature requests
- Discussions: для вопросов и обсуждений
- Email: contact@qrmenu.com (замените на реальный)

---

## 📄 Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под [MIT License](./LICENSE).

---

**Спасибо за ваш вклад! 🎉**
