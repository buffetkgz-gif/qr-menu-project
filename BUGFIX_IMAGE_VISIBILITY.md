# 🐛 Исправление: Фото блюд не отображаются в модальном окне редактирования

## Проблема

При открытии модального окна редактирования блюда текущее фото не отображалось, даже если оно было загружено.

## Причина

**Несоответствие имен полей между backend и frontend:**

- **Backend (Prisma schema):** Поле называется `image`
- **Frontend (React):** Компонент ожидает поле `imageUrl`

```javascript
// Frontend ожидал:
const [currentImageUrl, setCurrentImageUrl] = useState(dish?.imageUrl || null);

// Backend возвращал:
{
  id: "...",
  name: "Margherita Pizza",
  image: "/uploads/pizza.jpg",  // ❌ Не совпадает!
  ...
}
```

## Решение

Добавлен маппинг в backend контроллере для преобразования `image` → `imageUrl`:

### Файл: `backend/src/controllers/dish.controller.js`

#### 1. Метод `getDishes` (получение списка блюд)

```javascript
export const getDishes = async (req, res, next) => {
  try {
    const { categoryId } = req.params;

    const dishes = await prisma.dish.findMany({
      where: { categoryId },
      orderBy: { order: "asc" },
      include: {
        modifiers: {
          orderBy: { order: "asc" },
        },
      },
    });

    // ✅ Маппинг image → imageUrl
    const dishesWithImageUrl = dishes.map((dish) => ({
      ...dish,
      imageUrl: dish.image,
    }));

    res.json(dishesWithImageUrl);
  } catch (error) {
    next(error);
  }
};
```

#### 2. Метод `createDish` (создание блюда)

```javascript
res.status(201).json({
  ...dish,
  imageUrl: dish.image, // ✅ Добавлен маппинг
});
```

#### 3. Метод `updateDish` (обновление блюда)

```javascript
res.json({
  ...updatedDish,
  imageUrl: updatedDish.image, // ✅ Добавлен маппинг
});
```

#### 4. Метод `uploadDishImage` (загрузка фото)

```javascript
res.json({
  message: "Image uploaded successfully",
  imageUrl,
  dish: {
    ...updatedDish,
    imageUrl: updatedDish.image, // ✅ Добавлен маппинг
  },
});
```

#### 5. Метод `deleteDishImage` (удаление фото)

```javascript
res.json({
  message: "Image deleted successfully",
  dish: {
    ...updatedDish,
    imageUrl: updatedDish.image, // ✅ Добавлен маппинг (будет null)
  },
});
```

## Результат

✅ **Теперь фото блюд корректно отображается:**

1. **В списке блюд** - миниатюра 64x64px с зеленой рамкой
2. **В модальном окне редактирования** - полноразмерное изображение (w-full h-48)
3. **При загрузке нового фото** - предпросмотр и прогресс-бар
4. **При удалении фото** - корректное обновление состояния

## Тестирование

### Шаги для проверки:

1. Открыть http://localhost:5173/menu-management
2. Войти как `test@restaurant.com` / `test123`
3. Раскрыть категорию с блюдами
4. Нажать на блюдо с фото (зеленая рамка)
5. ✅ **Фото должно отображаться в модальном окне**

### Проверка в DevTools:

```javascript
// Открыть консоль браузера (F12)
// Проверить данные блюда:
console.log(dish);

// Должно быть:
{
  id: "...",
  name: "Margherita Pizza",
  image: "/uploads/pizza.jpg",
  imageUrl: "/uploads/pizza.jpg",  // ✅ Теперь есть!
  ...
}
```

## Альтернативные решения (не использованы)

### ❌ Вариант 1: Изменить frontend

```javascript
// Изменить все упоминания imageUrl на image
const [currentImageUrl, setCurrentImageUrl] = useState(dish?.image || null);
```

**Минусы:**

- Нужно менять много мест в коде
- Менее семантично (image - это имя поля БД, imageUrl - это URL изображения)

### ❌ Вариант 2: Изменить схему Prisma

```prisma
model Dish {
  imageUrl String?  // Переименовать поле
}
```

**Минусы:**

- Требует миграции БД
- Может сломать существующие данные
- Избыточно для простого маппинга

## Связанные файлы

- `backend/src/controllers/dish.controller.js` - Основное исправление
- `frontend/src/pages/MenuManagementPage.jsx` - Использует `imageUrl`
- `backend/prisma/schema.prisma` - Определение поля `image`

## Дата исправления

2025-01-10

## Автор

AI Assistant (Zencoder)
