# 🏗️ Архитектура QR Menu SaaS

Детальное описание архитектуры проекта.

---

## 📊 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Browser    │  │    Mobile    │  │   Tablet     │      │
│  │   Desktop    │  │    Phone     │  │              │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                            │                                 │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             │ HTTPS
                             │
┌────────────────────────────▼─────────────────────────────────┐
│                    FRONTEND LAYER (React)                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              React Application (Vite)                │    │
│  │                                                       │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │    │
│  │  │  Pages   │  │Components│  │  Store   │          │    │
│  │  │          │  │          │  │ (Zustand)│          │    │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘          │    │
│  │       │             │             │                 │    │
│  │       └─────────────┴─────────────┘                 │    │
│  │                     │                               │    │
│  │              ┌──────▼──────┐                        │    │
│  │              │   Services  │                        │    │
│  │              │   (Axios)   │                        │    │
│  │              └──────┬──────┘                        │    │
│  └─────────────────────┼───────────────────────────────┘    │
│                        │                                     │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         │ HTTP/REST API
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                   BACKEND LAYER (Node.js)                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           Express.js Application                     │    │
│  │                                                       │    │
│  │  ┌──────────────────────────────────────────────┐   │    │
│  │  │            Middleware Stack                   │   │    │
│  │  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│   │    │
│  │  │  │  CORS  │→│ Helmet │→│  Rate  │→│  Auth  ││   │    │
│  │  │  │        │ │        │ │ Limit  │ │  JWT   ││   │    │
│  │  │  └────────┘ └────────┘ └────────┘ └────────┘│   │    │
│  │  └──────────────────┬───────────────────────────┘   │    │
│  │                     │                               │    │
│  │  ┌──────────────────▼───────────────────────────┐   │    │
│  │  │              Routes Layer                     │   │    │
│  │  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│   │    │
│  │  │  │  Auth  │ │Restaurant│ │  Menu  │ │ Admin ││   │    │
│  │  │  │ Routes │ │  Routes  │ │ Routes │ │Routes ││   │    │
│  │  │  └───┬────┘ └────┬─────┘ └───┬────┘ └───┬───┘│   │    │
│  │  └──────┼───────────┼───────────┼──────────┼─────┘   │    │
│  │         │           │           │          │         │    │
│  │  ┌──────▼───────────▼───────────▼──────────▼─────┐   │    │
│  │  │           Controllers Layer                    │   │    │
│  │  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │   │    │
│  │  │  │  Auth  │ │Restaurant│ │  Menu  │ │ Admin │ │   │    │
│  │  │  │  Ctrl  │ │   Ctrl   │ │  Ctrl  │ │  Ctrl │ │   │    │
│  │  │  └───┬────┘ └────┬─────┘ └───┬────┘ └───┬───┘ │   │    │
│  │  └──────┼───────────┼───────────┼──────────┼──────┘   │    │
│  │         │           │           │          │          │    │
│  │  ┌──────▼───────────▼───────────▼──────────▼──────┐   │    │
│  │  │            Prisma ORM Layer                     │   │    │
│  │  │         (Database Abstraction)                  │   │    │
│  │  └──────────────────┬──────────────────────────────┘   │    │
│  └─────────────────────┼───────────────────────────────────┘    │
│                        │                                         │
└────────────────────────┼─────────────────────────────────────────┘
                         │
                         │ SQL
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                    DATABASE LAYER                             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              PostgreSQL Database                     │    │
│  │                                                       │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │    │
│  │  │  User  │ │Restaurant│Subscription│Category│       │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘       │    │
│  │  ┌────────┐ ┌────────┐                              │    │
│  │  │  Dish  │ │Modifier│                              │    │
│  │  └────────┘ └────────┘                              │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │    SMTP      │  │   WhatsApp   │  │ File Storage │      │
│  │   (Email)    │  │  (Ordering)  │  │   (Local)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных

### 1. Регистрация пользователя

```
┌─────────┐
│ Browser │
└────┬────┘
     │ 1. POST /api/auth/register
     │    { email, password, restaurantName, ... }
     ▼
┌─────────────┐
│   Frontend  │
│   Service   │
└────┬────────┘
     │ 2. HTTP Request
     ▼
┌─────────────┐
│   Express   │
│  Middleware │
└────┬────────┘
     │ 3. Validate & Rate Limit
     ▼
┌─────────────┐
│    Auth     │
│ Controller  │
└────┬────────┘
     │ 4. Hash password (bcrypt)
     │ 5. Create User
     │ 6. Create Restaurant
     │ 7. Create Subscription (TRIAL)
     ▼
┌─────────────┐
│   Prisma    │
│     ORM     │
└────┬────────┘
     │ 8. SQL INSERT
     ▼
┌─────────────┐
│ PostgreSQL  │
└────┬────────┘
     │ 9. Return created records
     ▼
┌─────────────┐
│    Auth     │
│ Controller  │
└────┬────────┘
     │ 10. Generate JWT token
     │ 11. Send welcome email
     ▼
┌─────────────┐
│   Frontend  │
└────┬────────┘
     │ 12. Store token in authStore
     │ 13. Redirect to /dashboard
     ▼
┌─────────┐
│Dashboard│
└─────────┘
```

### 2. Просмотр меню (публичный)

```
┌─────────┐
│ Client  │
│ (QR)    │
└────┬────┘
     │ 1. Scan QR code
     │    → /menu/subdomain
     ▼
┌─────────────┐
│   Frontend  │
│  MenuPage   │
└────┬────────┘
     │ 2. GET /api/restaurants/:subdomain
     ▼
┌─────────────┐
│   Express   │
│  (No Auth)  │
└────┬────────┘
     │ 3. No authentication required
     ▼
┌─────────────┐
│ Restaurant  │
│ Controller  │
└────┬────────┘
     │ 4. Find restaurant by subdomain
     │ 5. Include categories, dishes, modifiers
     ▼
┌─────────────┐
│   Prisma    │
└────┬────────┘
     │ 6. SQL SELECT with JOINs
     ▼
┌─────────────┐
│ PostgreSQL  │
└────┬────────┘
     │ 7. Return restaurant data
     ▼
┌─────────────┐
│   Frontend  │
│  MenuPage   │
└────┬────────┘
     │ 8. Render menu
     │ 9. Display categories
     │ 10. Display dishes
     ▼
┌─────────┐
│ Client  │
│ Views   │
└─────────┘
```

### 3. Добавление блюда в корзину

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. Click "Add to Cart"
     ▼
┌─────────────┐
│  DishModal  │
└────┬────────┘
     │ 2. Select modifiers
     │ 3. Click "Add"
     ▼
┌─────────────┐
│  cartStore  │
│  (Zustand)  │
└────┬────────┘
     │ 4. addToCart(dish, modifiers)
     │ 5. Calculate total price
     │ 6. Update cart state
     ▼
┌─────────────┐
│    Cart     │
│ Component   │
└────┬────────┘
     │ 7. Re-render with new item
     │ 8. Show updated total
     ▼
┌─────────┐
│ Client  │
│ Sees    │
└─────────┘
```

### 4. Заказ через WhatsApp

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. Click "Order via WhatsApp"
     ▼
┌─────────────┐
│    Cart     │
│ Component   │
└────┬────────┘
     │ 2. Get cart items from cartStore
     │ 3. Format order message:
     │    "Order from [Restaurant]
     │     - Dish 1 (modifiers) x2
     │     - Dish 2 x1
     │     Total: $XX.XX"
     ▼
┌─────────────┐
│  WhatsApp   │
│     API     │
└────┬────────┘
     │ 4. Open WhatsApp with pre-filled message
     │    wa.me/[phone]?text=[message]
     ▼
┌─────────┐
│WhatsApp │
│   App   │
└─────────┘
```

### 5. Создание блюда (владелец)

```
┌─────────┐
│  Owner  │
└────┬────┘
     │ 1. POST /api/dishes
     │    Authorization: Bearer [token]
     │    { name, price, categoryId, ... }
     ▼
┌─────────────┐
│   Express   │
│  Middleware │
└────┬────────┘
     │ 2. Auth middleware
     │ 3. Verify JWT token
     │ 4. Check user role (OWNER)
     ▼
┌─────────────┐
│    Menu     │
│ Controller  │
└────┬────────┘
     │ 5. Validate input
     │ 6. Check category ownership
     │ 7. Create dish
     ▼
┌─────────────┐
│   Prisma    │
└────┬────────┘
     │ 8. SQL INSERT
     ▼
┌─────────────┐
│ PostgreSQL  │
└────┬────────┘
     │ 9. Return created dish
     ▼
┌─────────────┐
│   Frontend  │
└────┬────────┘
     │ 10. Update UI
     │ 11. Show success message
     ▼
┌─────────┐
│  Owner  │
│  Sees   │
└─────────┘
```

### 6. Загрузка изображения

```
┌─────────┐
│  Owner  │
└────┬────┘
     │ 1. POST /api/dishes/:id/upload-image
     │    Authorization: Bearer [token]
     │    Content-Type: multipart/form-data
     │    Body: { image: [file] }
     ▼
┌─────────────┐
│   Express   │
│  Middleware │
└────┬────────┘
     │ 2. Auth middleware
     │ 3. Multer middleware
     │ 4. Validate file (type, size)
     ▼
┌─────────────┐
│   Multer    │
└────┬────────┘
     │ 5. Save file to disk
     │    → backend/uploads/dishes/[filename]
     ▼
┌─────────────┐
│    Menu     │
│ Controller  │
└────┬────────┘
     │ 6. Get file path
     │ 7. Update dish.image in DB
     ▼
┌─────────────┐
│   Prisma    │
└────┬────────┘
     │ 8. SQL UPDATE
     ▼
┌─────────────┐
│ PostgreSQL  │
└────┬────────┘
     │ 9. Return updated dish
     ▼
┌─────────────┐
│   Frontend  │
└────┬────────┘
     │ 10. Display uploaded image
     ▼
┌─────────┐
│  Owner  │
│  Sees   │
└─────────┘
```

---

## 🔐 Безопасность

### Уровни защиты

```
┌─────────────────────────────────────────────────────────┐
│                    Security Layers                       │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Layer 1: Network Security                               │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • HTTPS/TLS encryption                           │    │
│  │ • CORS policy                                    │    │
│  │ • Helmet (security headers)                      │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Layer 2: Rate Limiting                                  │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • Auth endpoints: 5 req/15min                    │    │
│  │ • General API: 100 req/15min                     │    │
│  │ • DDoS protection                                │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Layer 3: Authentication                                 │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • JWT tokens (7 days expiry)                     │    │
│  │ • bcrypt password hashing (10 rounds)            │    │
│  │ • Token verification on protected routes         │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Layer 4: Authorization                                  │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • Role-based access control (OWNER/ADMIN)        │    │
│  │ • Resource ownership verification                │    │
│  │ • Admin-only endpoints protection                │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Layer 5: Input Validation                               │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • Request body validation                        │    │
│  │ • File upload validation (type, size)            │    │
│  │ • SQL injection prevention (Prisma)              │    │
│  │ • XSS prevention                                 │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Layer 6: Data Protection                                │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • Environment variables for secrets              │    │
│  │ • .gitignore for sensitive files                 │    │
│  │ • Database connection encryption                 │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 📦 Модульная структура

### Frontend модули

```
frontend/src/
│
├── components/          # Переиспользуемые компоненты
│   ├── PrivateRoute     # HOC для защищенных маршрутов
│   ├── AdminRoute       # HOC для админ маршрутов
│   ├── BannerSlider     # Слайдер баннеров (Swiper)
│   ├── DishCard         # Карточка блюда
│   ├── DishModal        # Модальное окно блюда
│   └── Cart             # Корзина
│
├── pages/               # Страницы приложения
│   ├── HomePage         # Лендинг
│   ├── LoginPage        # Вход
│   ├── RegisterPage     # Регистрация
│   ├── MenuPage         # Публичное меню
│   ├── DashboardPage    # Дашборд владельца
│   ├── AdminPage        # Админ-панель
│   └── NotFoundPage     # 404
│
├── services/            # API сервисы
│   ├── api.js           # Axios instance + interceptors
│   ├── auth.service     # Аутентификация
│   ├── restaurant.service # Рестораны
│   └── menu.service     # Меню
│
├── store/               # State management
│   ├── authStore        # Аутентификация (persistent)
│   └── cartStore        # Корзина
│
├── App.jsx              # Главный компонент + роутинг
└── main.jsx             # Точка входа
```

### Backend модули

```
backend/src/
│
├── controllers/         # Бизнес-логика
│   ├── auth.controller      # Регистрация, вход
│   ├── restaurant.controller # CRUD ресторанов
│   ├── menu.controller      # CRUD меню
│   └── admin.controller     # Админ функции
│
├── routes/              # API маршруты
│   ├── auth.routes      # /api/auth/*
│   ├── restaurant.routes # /api/restaurants/*
│   ├── menu.routes      # /api/categories/*, /api/dishes/*
│   └── admin.routes     # /api/admin/*
│
├── middleware/          # Middleware
│   ├── auth.js          # JWT verification
│   ├── errorHandler.js  # Error handling
│   ├── rateLimiter.js   # Rate limiting
│   └── upload.js        # File upload (Multer)
│
├── utils/               # Утилиты
│   ├── email.js         # Email отправка
│   └── subscription.js  # Управление подписками
│
├── scripts/             # Скрипты
│   ├── createAdmin.js   # Создание админа
│   └── seedDatabase.js  # Seed данные
│
└── server.js            # Точка входа
```

---

## 🗄️ Модель данных

### Entity Relationship Diagram

```
┌─────────────────┐
│      User       │
├─────────────────┤
│ id (PK)         │
│ email           │
│ password        │
│ name            │
│ role            │
│ restaurantId(FK)│
└────────┬────────┘
         │ 1:1
         │
         ▼
┌─────────────────┐
│   Restaurant    │
├─────────────────┤
│ id (PK)         │
│ name            │
│ subdomain       │
│ description     │
│ address         │
│ phone           │
│ whatsapp        │
│ instagram       │
│ facebook        │
│ banners         │
│ deliveryEnabled │
│ deliveryFee     │
│ minOrderAmount  │
│ defaultLanguage │
│ subscriptionId  │
└────┬────────┬───┘
     │ 1:1    │ 1:N
     │        │
     ▼        ▼
┌────────┐  ┌──────────┐
│Subscrip│  │ Category │
│tion    │  ├──────────┤
├────────┤  │ id (PK)  │
│id (PK) │  │ name     │
│plan    │  │ displayOr│
│status  │  │ restaurant│
│startDat│  │ Id (FK)  │
│endDate │  └────┬─────┘
└────────┘       │ 1:N
                 │
                 ▼
            ┌─────────┐
            │  Dish   │
            ├─────────┤
            │ id (PK) │
            │ name    │
            │ descript│
            │ price   │
            │ image   │
            │ displayO│
            │ category│
            │ Id (FK) │
            └────┬────┘
                 │ 1:N
                 │
                 ▼
            ┌─────────┐
            │Modifier │
            ├─────────┤
            │ id (PK) │
            │ name    │
            │ priceMod│
            │ dishId  │
            │ (FK)    │
            └─────────┘
```

---

## 🔄 State Management (Frontend)

### Zustand Stores

```
┌─────────────────────────────────────────────────────────┐
│                      authStore                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  State:                                                  │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • user: { id, email, name, role, restaurant }   │    │
│  │ • token: string | null                           │    │
│  │ • isAuthenticated: boolean                       │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Actions:                                                │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • login(email, password)                         │    │
│  │ • register(data)                                 │    │
│  │ • logout()                                       │    │
│  │ • setUser(user)                                  │    │
│  │ • setToken(token)                                │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Persistence: localStorage                               │
│                                                           │
└───────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                      cartStore                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  State:                                                  │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • items: [                                       │    │
│  │     {                                            │    │
│  │       dish: { id, name, price, ... },           │    │
│  │       modifiers: [...],                          │    │
│  │       quantity: number,                          │    │
│  │       totalPrice: number                         │    │
│  │     }                                            │    │
│  │   ]                                              │    │
│  │ • totalItems: number                             │    │
│  │ • totalPrice: number                             │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Actions:                                                │
│  ┌─────────────────────────────────────────────────┐    │
│  │ • addToCart(dish, modifiers)                     │    │
│  │ • removeFromCart(itemId)                         │    │
│  │ • updateQuantity(itemId, quantity)               │    │
│  │ • clearCart()                                    │    │
│  │ • calculateTotals()                              │    │
│  └─────────────────────────────────────────────────┘    │
│                                                           │
│  Persistence: none (session only)                        │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────────────────────────┐
│                      Cloudflare                          │
│                   (DNS + CDN + SSL)                      │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌───────────────┐         ┌───────────────┐
│    Vercel     │         │    Render     │
│   (Frontend)  │         │   (Backend)   │
│               │         │               │
│ • React App   │         │ • Express API │
│ • Static      │         │ • Node.js     │
│ • CDN         │         │ • Uploads     │
└───────────────┘         └───────┬───────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
            ┌───────────────┐         ┌───────────────┐
            │   Supabase    │         │  Cloudinary   │
            │  (PostgreSQL) │         │   (Images)    │
            │               │         │   [Optional]  │
            │ • Database    │         │               │
            │ • Backups     │         │ • CDN         │
            └───────────────┘         └───────────────┘
```

---

## 📈 Scalability

### Horizontal Scaling

```
┌─────────────────────────────────────────────────────────┐
│                    Load Balancer                         │
└────────┬────────────┬────────────┬────────────┬─────────┘
         │            │            │            │
         ▼            ▼            ▼            ▼
    ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
    │Backend │  │Backend │  │Backend │  │Backend │
    │Instance│  │Instance│  │Instance│  │Instance│
    │   1    │  │   2    │  │   3    │  │   N    │
    └────┬───┘  └────┬───┘  └────┬───┘  └────┬───┘
         │            │            │            │
         └────────────┴────────────┴────────────┘
                      │
                      ▼
              ┌───────────────┐
              │  Redis Cache  │
              │  (Shared)     │
              └───────┬───────┘
                      │
                      ▼
              ┌───────────────┐
              │  PostgreSQL   │
              │  (Primary)    │
              └───────┬───────┘
                      │
         ┌────────────┴────────────┐
         ▼                         ▼
    ┌────────┐              ┌────────┐
    │Postgres│              │Postgres│
    │Replica │              │Replica │
    │   1    │              │   2    │
    └────────┘              └────────┘
```

---

## 🔍 Monitoring & Logging

### Observability Stack

```
┌─────────────────────────────────────────────────────────┐
│                    Application                           │
└────────┬────────────┬────────────┬────────────┬─────────┘
         │            │            │            │
         │ Logs       │ Metrics    │ Traces     │ Errors
         │            │            │            │
         ▼            ▼            ▼            ▼
    ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
    │Winston │  │Prometheus│ │ Jaeger │  │ Sentry │
    │        │  │        │  │        │  │        │
    └────┬───┘  └────┬───┘  └────┬───┘  └────┬───┘
         │            │            │            │
         └────────────┴────────────┴────────────┘
                      │
                      ▼
              ┌───────────────┐
              │   Grafana     │
              │  (Dashboard)  │
              └───────────────┘
```

---

## 🎯 Performance Optimization

### Caching Strategy

```
┌─────────────────────────────────────────────────────────┐
│                    Client Request                        │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ CDN Cache    │ ← Static assets
                  │ (Cloudflare) │   (images, CSS, JS)
                  └──────┬───────┘
                         │ Miss
                         ▼
                  ┌──────────────┐
                  │ Redis Cache  │ ← API responses
                  │ (In-memory)  │   (menu data, etc)
                  └──────┬───────┘
                         │ Miss
                         ▼
                  ┌──────────────┐
                  │  PostgreSQL  │ ← Source of truth
                  │  (Database)  │
                  └──────────────┘
```

---

**Архитектура готова к масштабированию и production использованию! 🚀**
