# 📍 Настройка геолокации и выбора ближайшего ресторана

> ⚠️ **СТАТУС:** Функция определения геолокации для клиентов **ОТКЛЮЧЕНА**.
> Backend API и поля в БД готовы для будущего использования.

## 🎯 Что было добавлено

Подготовлена система определения ближайшего ресторана для пользователей с учетом геолокации и зон доставки.

### ✨ Новые функции:

1. **Геолокация пользователя**

   - Автоматическое определение координат через GPS браузера
   - Возможность ввода адреса вручную (требует интеграции Geocoding API)

2. **Поиск ближайших ресторанов**

   - Расчет расстояния по формуле Haversine
   - Сортировка по расстоянию
   - Проверка зон доставки

3. **Зоны доставки**

   - Настраиваемый радиус доставки для каждого ресторана
   - Автоматическая проверка доступности доставки

4. **База данных клиентов**
   - Сохранение адресов для авторизованных пользователей
   - История заказов

---

## 📦 Установка и настройка

### 1️⃣ Применить миграцию базы данных

```bash
cd backend
npx prisma migrate dev --name add_geolocation_features
npx prisma generate
```

### 2️⃣ Перезапустить сервер

```bash
# Остановить текущий процесс (Ctrl+C)
# Запустить снова
npm run dev
```

---

## 🗄️ Изменения в базе данных

### Модель `Restaurant`

Добавлены поля:

- `latitude: Float?` - Широта ресторана
- `longitude: Float?` - Долгота ресторана
- `deliveryRadius: Float?` - Радиус доставки в км

### Модель `Order`

Добавлены поля:

- `deliveryAddress: String?` - Адрес доставки
- `deliveryLatitude: Float?` - Координаты доставки
- `deliveryLongitude: Float?` - Координаты доставки
- `customerId: String?` - Связь с клиентом

### Новые модели:

**Customer** - Клиенты ресторанов

- `id` - ID клиента
- `phone` - Телефон (уникальный)
- `name` - Имя
- `email` - Email
- `savedAddresses` - Сохраненные адреса
- `orders` - Заказы

**CustomerAddress** - Сохраненные адреса

- `id` - ID адреса
- `customerId` - ID клиента
- `address` - Адрес (текст)
- `latitude` - Широта
- `longitude` - Долгота
- `label` - Метка ("Дом", "Работа")
- `isDefault` - Адрес по умолчанию

---

## 🔧 API Endpoints

### Поиск ближайших ресторанов

```http
GET /api/geolocation/nearby?latitude=55.7558&longitude=37.6173&maxDistance=50
```

**Параметры:**

- `latitude` - Широта пользователя (обязательно)
- `longitude` - Долгота пользователя (обязательно)
- `maxDistance` - Максимальное расстояние в км (по умолчанию: 50)

**Ответ:**

```json
{
  "userLocation": {
    "latitude": 55.7558,
    "longitude": 37.6173
  },
  "count": 3,
  "restaurants": [
    {
      "id": "...",
      "name": "Ресторан 1",
      "distance": 1.23,
      "isInDeliveryZone": true,
      "deliveryAvailable": true,
      "deliveryFee": 300,
      "deliveryRadius": 5
    }
  ]
}
```

### Найти ближайший ресторан

```http
GET /api/geolocation/nearest?latitude=55.7558&longitude=37.6173
```

### Проверить доступность доставки

```http
GET /api/geolocation/check-delivery?restaurantId=xxx&latitude=55.7558&longitude=37.6173
```

---

## 🖥️ Использование на Frontend

> ⚠️ **ТЕКУЩИЙ РЕЖИМ:** Геолокация для клиентов ОТКЛЮЧЕНА

### Как клиенты попадают в меню ресторана

Клиенты переходят **напрямую** по ссылкам:

- `https://ваш-домен/menu/moya-picca`
- `https://ваш-домен/sushi-bar`
- `https://ваш-домен/pizza-house`

Владельцы распространяют ссылки через:

- 📱 WhatsApp рассылки
- 📸 Instagram профиль / Stories
- 🏪 QR-коды на столах и у входа
- 👥 Социальные сети

### 🔮 Будущая функция (если потребуется включить геолокацию)

<details>
<summary>Нажмите для просмотра инструкции по включению геолокации</summary>

Если в будущем понадобится страница `/select-restaurant` с автоматическим определением ближайшего ресторана:

**Что нужно сделать:**

1. Создать компонент `frontend/src/components/ClientRestaurantSelector.jsx` с запросом GPS
2. Добавить маршрут в `App.jsx`:
   ```jsx
   <Route path="/select-restaurant" element={<ClientRestaurantSelector />} />
   ```
3. Компонент будет использовать готовый API `/api/geolocation/nearby`
4. Показывать список ресторанов с расстояниями и зонами доставки

**Backend API уже готов и работает!** Нужно только создать Frontend компонент.

</details>

---

## 👨‍💼 Настройка для владельцев ресторанов

### Как установить координаты ресторана:

1. Перейти в **Настройки ресторана** (`/settings`)
2. Раздел **"Настройки доставки"**
3. Включить доставку (галочка)
4. Прокрутить до **"Геолокация и зона доставки"**
5. Указать:
   - **Широта (Latitude)** - например: `55.751244`
   - **Долгота (Longitude)** - например: `37.618423`
   - **Радиус доставки (км)** - например: `5`

### Как найти координаты:

#### Google Maps:

1. Открыть [Google Maps](https://www.google.com/maps)
2. Найти ваш ресторан
3. Кликнуть правой кнопкой мыши на точку
4. Выбрать первую строку (координаты) - они скопируются
5. Формат: `55.751244, 37.618423`

#### Яндекс Карты:

1. Открыть [Яндекс Карты](https://yandex.ru/maps)
2. Найти ваш ресторан
3. Кликнуть правой кнопкой мыши
4. Выбрать "Что здесь?"
5. Внизу появятся координаты

---

## 🧪 Тестирование

### 1. Тестовые координаты

**Москва, Красная площадь:**

- Latitude: `55.7558`
- Longitude: `37.6173`

**Санкт-Петербург, Дворцовая площадь:**

- Latitude: `59.9386`
- Longitude: `30.3141`

### 2. Проверка API

```bash
# Найти рестораны рядом с Красной площадью
curl "http://localhost:5000/api/geolocation/nearby?latitude=55.7558&longitude=37.6173&maxDistance=10"

# Найти ближайший ресторан
curl "http://localhost:5000/api/geolocation/nearest?latitude=55.7558&longitude=37.6173"
```

### 3. Тестирование на Frontend

1. Открыть `http://localhost:5173/select-restaurant`
2. Разрешить доступ к геолокации
3. Проверить, что рестораны загружаются
4. Проверить сортировку по расстоянию
5. Проверить статус доставки

---

## 🔮 Будущие улучшения

### 1. Geocoding API

Для конвертации адреса в координаты можно использовать:

- **Google Geocoding API** (платно после лимита)
- **Yandex Geocoding API** (бесплатно до 25000 запросов/день)
- **Nominatim** (OpenStreetMap, бесплатно)

### 2. Интерактивная карта

Добавить карту с маркерами ресторанов:

- Google Maps API
- Yandex Maps API
- Leaflet + OpenStreetMap

### 3. Авторизация клиентов

- SMS-коды для верификации
- Сохранение нескольких адресов
- История заказов

### 4. Push-уведомления

- Уведомления о статусе заказа
- Информация о курьере

---

## ⚠️ Важные замечания

1. **Браузерная геолокация** требует HTTPS (кроме localhost)
2. **Точность GPS** может варьироваться (обычно ±10-50 метров)
3. **Формула Haversine** дает расстояние "по прямой", не учитывая дороги
4. **Координаты** должны быть в формате Decimal Degrees (не DMS)
5. **Радиус доставки** - это приблизительное значение

---

## 📝 Примеры использования

### Пример 1: Добавление геолокации существующему ресторану

```sql
-- Обновить координаты через SQL (если нужно)
UPDATE Restaurant
SET latitude = 55.7558,
    longitude = 37.6173,
    deliveryRadius = 5
WHERE id = 'restaurant_id';
```

### Пример 2: Создание ресторана с координатами

```javascript
const restaurant = await prisma.restaurant.create({
  data: {
    name: "Пицца Маргарита",
    subdomain: "pizza-margarita",
    address: "Москва, ул. Тверская, 1",
    latitude: 55.7558,
    longitude: 37.6173,
    deliveryEnabled: true,
    deliveryRadius: 5,
    deliveryFee: 300,
    ownerId: userId,
  },
});
```

---

## 🆘 Решение проблем

### Проблема: Геолокация не работает

**Решение:**

- Убедитесь что используется HTTPS или localhost
- Проверьте разрешения браузера
- Попробуйте другой браузер

### Проблема: Рестораны не находятся

**Решение:**

- Проверьте что у ресторанов установлены координаты
- Увеличьте `maxDistance` параметр
- Проверьте что координаты корректны

### Проблема: Неправильное расстояние

**Решение:**

- Проверьте формат координат (Decimal Degrees)
- Убедитесь что широта и долгота не перепутаны
- Проверьте что `deliveryRadius` в километрах

---

## 📚 Полезные ссылки

- [Google Maps](https://www.google.com/maps)
- [Яндекс Карты](https://yandex.ru/maps)
- [Haversine Formula](https://en.wikipedia.org/wiki/Haversine_formula)
- [MDN Geolocation API](https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API)

---

## ✅ Чеклист внедрения

- [ ] Применить миграцию базы данных
- [ ] Перезапустить backend сервер
- [ ] Добавить координаты ресторанам через настройки
- [ ] Протестировать `/select-restaurant` страницу
- [ ] Проверить API endpoints
- [ ] (Опционально) Интегрировать Geocoding API
- [ ] (Опционально) Добавить карту

---

**Дата создания:** 2024  
**Автор:** OimoQR Team
